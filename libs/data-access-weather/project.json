{
  "name": "data-access-weather",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/data-access-weather/src",
  "prefix": "da",
  "projectType": "library",
  "tags": ["type:data-access", "scope:weather"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/data-access-weather"
      }
    }
  }
}
